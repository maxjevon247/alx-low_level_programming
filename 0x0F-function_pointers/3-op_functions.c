#include <stdio.h>

/**
 * op_add - Function that returns the sum of two values
 * @a: first srgument
 * @b: second argument
 * Return: The sum of two arguments
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - Function that returns the difference of two values
 * @a: first srgument
 * @b: second argument
 * Return: The difference of two arguments
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - Function that returns the product of two values
 * @a: first srgument
 * @b: second argument
 * Return: The product of two arguments
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - Function that returns the quotient of two values
 * @a: first srgument
 * @b: second argument
 * Return: The qoutient of two arguments
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - Function that returns the remainder of two values
 * @a: first srgument
 * @b: second argument
 * Return: The remainder of two arguments
 */
int op_mod(int a, int b)
{
	return (a % b);
}
