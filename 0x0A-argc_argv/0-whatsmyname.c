#include "main.h"

/**
 * whatsmyname -program that prints its name, followed by a new line
 *
 * Return: Always return 0
 */
int main(int argc, char *argv[])
{
	putchar("Program's name is: %s\n",arg[])
		return 0
}
